# https://taskfile.dev

version: '3'

dotenv: ['.env']

tasks:
  gen-gubernator:
    cmds:
      - protoc --go_out=./ --go_opt=paths=source_relative
        --go-grpc_out=./ --go-grpc_opt=paths=source_relative
        -I=./ gubernator/*.proto
